# AI Agent 动态爬虫操作指南 (Autonomous Scraper SOP)

本指南为 AI Agent 提供了一套标准化的操作流程，指导其在面对未知的供应商或比价网站时，如何自主完成从“规则发现”到“数据提取”的全过程。

## 第一阶段：合规性与规则检查 (Bot & Rule Inspection)
在发起任何请求前，Agent 必须先明确网站的“边界”：
1.  **检查 `robots.txt`**：访问 `domain.com/robots.txt`，识别允许（Allow）和禁止（Disallow）爬取的路径。
2.  **识别反爬机制**：通过一次轻量级请求判断网站是否部署了 Cloudflare、CAPTCHA 或强制登录。
3.  **制定爬取策略**：根据网站限制设置合理的 `User-Agent`、请求频率（Delay）以及是否需要模拟 JavaScript 执行（如使用 Selenium）。

## 第二阶段：网站结构解析 (Site Analysis)
Agent 应通过获取网页的 HTML 骨架（DOM Tree）来理解其信息布局：
1.  **识别页面类型**：判断当前页面是“搜索结果列表页”还是“供应商详情页”。
2.  **定位核心容器**：
    *   **供应商名称**：寻找 `<h1>` 或带有 `company-name` 类名的标签。
    *   **指标数据区**：寻找包含“成立年份”、“员工数”、“地址”等关键词的邻近 DOM 节点。
    *   **产品列表区**：寻找包含大量重复结构（如 `<li>` 或 `div.product-item`）的容器。

## 第三阶段：信息分类与字段提取 (Information Classification)
将解析到的信息按照 `DESIGN_SPEC.md` 标准进行分类，明确哪些数据是“可直接爬取的”，哪些是“需要推断的”：
1.  **直接提取项**：名称、城市、网站链接。
2.  **模式匹配项**：
    *   **年份**：使用正则 `\b(19|20)\d{2}\b` 匹配成立时间。
    *   **规模**：匹配带有“Mitarbeiter”、“Staff”或数字区间（如 50-200）的文本。
3.  **语义清理项**：
    *   **简介**：提取长段落文本，并剔除其中的导航栏、页脚及广告（如 App 推广、无关产品推荐）。

## 第四阶段：动态设置爬虫 (Scraper Configuration)
根据上述分析，Agent 动态配置爬虫逻辑：
1.  **定义 RULES 字典**：生成一套针对该网站的 CSS 选择器或 XPath 规则。
2.  **设置交互动作**：如果信息被隐藏（如“点击查看更多”），需配置相应的模拟点击脚本。
3.  **配置过滤器**：设置路径黑名单（如排除 `/ads/`, `/recommendations/`）以确保产品列表的纯净。

## 第五阶段：执行抓取与数据返回 (Execution & Output)
1.  **执行抓取**：运行配置好的爬虫逻辑，并实时监控成功率。
2.  **数据校验**：检查返回的 JSON 是否符合 Schema 要求。
3.  **异常处理**：
    *   若字段缺失：记录缺失原因（如 `not found on page`）。
    *   若遭遇封锁：立即停止并返回已获取的摘要信息，标记 `status: degraded`。


